<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/nav_graph.xml"
    app:startDestination="@id/fragmentBoard">

    <fragment
        android:id="@+id/fragmentBoard"
        android:name="com.example.app.FragmentBoard"
        android:label="FragmentBoard" />
    <fragment
        android:id="@+id/fragmentHome"
        android:name="FragmentHome"
        android:label="FragmentHome" >
        <action
            android:id="@+id/action_fragmentHome_to_fragmentBoard"
            app:destination="@id/fragmentBoard" />
    </fragment>
</navigation>